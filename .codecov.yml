# https://docs.codecov.io/docs/codecovyml-reference

# We have
#   8 * [number of basic OS] + 4 * [number of additional OS]
# with
# [number of basic OS] = 1 (Linux)
# [number of additional OS] = 2 (Windows, MacOS)
# parallel jobs in ci.yml
codecov:
  notify:
    after_n_builds: 16
comment:
  after_n_builds: 16

coverage:
  range: 70..95
  round: down
  precision: 2

github_checks:
  annotations: false
